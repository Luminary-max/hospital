# 医院门诊系统角色划分分析

## 当前结论

在当前需求下，护士角色仍然需要保留，但不应继续混在“管理员”这个泛角色里。收费员也需要作为独立业务角色或管理员子角色存在。药师同理，应作为药房业务角色存在。

## 角色建议

| 角色 | 是否需要 | 核心职责 | 典型功能 |
| --- | --- | --- | --- |
| 系统管理员 | 需要 | 账号、权限、基础数据、系统配置 | 管理员分权限、医生/患者/科室/药品基础维护 |
| 护士/分诊员 | 需要 | 门诊现场分诊与候诊秩序维护 | 分诊记录、生命体征录入、分诊等级、排队协助 |
| 医生 | 需要 | 临床诊疗与医嘱下达 | AI辅助诊断、健康档案、病历、处方、检查、转诊 |
| 药师 | 需要 | 药房发药与用药安全 | 发药复核、报告解析辅助、处方合理性检查 |
| 收费员 | 需要 | 费用结算、退费、票据 | 收费结算、发票、退费审批协助、医保费用试算 |
| 患者 | 需要 | 自助就诊服务 | 预约挂号、排队预测、健康档案、报告解析、医保试算、公告、转诊记录 |

## 为什么护士仍然需要

图片中的“管理员分权限：药师，护士”说明管理员不是一个实际业务岗位，而是权限分配者。分诊、生命体征采集、候诊队列现场维护这些工作在现实门诊中通常由护士或分诊员完成，和系统管理员的数据维护职责不同。因此护士应保留，并建议限制到分诊、排队协助、患者现场状态更新等功能。

## 为什么需要收费员

当前系统已有挂号费、药费、检查费、发票、退费、医保结算等闭环。收费员承担的是财务岗位职责，不应由系统管理员长期代办。新增“医保试算”和既有“收费结算、发票、退费审批”都说明收费员或财务角色是合理的。

## 本次代码中的处理

本次实现已将护士、药师、收费员升级为与管理员、医生、患者平级的登录角色，落入 `staff_user` 表，并通过 `/staff/login` 独立登录。`admin_role_permission` 继续用于管理员维护这些岗位角色的功能权限范围。

## 权限如何生效

护士、药师、收费员的“账号身份”和“功能权限”分开维护：

- `staff_user`：维护职工账号、密码、姓名、角色编码、启用状态。
- `admin_role_permission`：维护角色对应的功能权限，例如 `triage`、`queue`、`pharmacy`、`health_profile`、`report`、`prescription_review`、`cashier`、`insurance`。

前端管理员布局会调用 `/smart/role/current` 读取当前角色权限，并按权限决定菜单是否展示。例如护士只有 `triage,queue` 时，只展示分诊记录、排队叫号和公告；药师有 `pharmacy,health_profile,report,prescription_review` 时，展示药房管理和智慧扩展；收费员有 `cashier,insurance` 时，展示收费结算和医保试算。

后端新增智慧扩展接口也会读取当前角色和权限进行校验，不能只依赖前端隐藏菜单。患者端健康档案接口会强制使用 token 中的本人 `pId`，不会返回其他患者档案。
