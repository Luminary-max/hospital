# 医院门诊管理系统 — 整体架构与实现过程

---

## 一、系统概述

本系统是一个基于 **Spring Boot + Vue 2 + MySQL** 的 B/S 架构医院门诊管理系统，支持管理员、医生、患者三种角色，覆盖从挂号、分诊、接诊、开方、缴费、发药到退费的全流程。

---

## 二、技术架构

```
┌─────────────────────────────────────────────────┐
│                    前端层 (Vue 2)                  │
│  Element UI + Axios + Vue Router + ECharts       │
│  56个 .vue 页面组件                                │
│  localhost:8089                                   │
├─────────────────────────────────────────────────┤
│                  HTTP / JSON                      │
├─────────────────────────────────────────────────┤
│                  后端层 (Spring Boot 2.2.4)        │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────────┐  │
│  │ Controllers│ │ Services │ │ MyBatis-Plus     │  │
│  │ (24个)     │ │ (24个)   │ │ Mappers (28个)   │  │
│  └──────────┘ └──────────┘ └──────────────────┘  │
│          │           │              │              │
│          └───────────┴──────────────┘              │
│                      │                             │
│     JWT 鉴权拦截器 + Redis(可选)                     │
│     localhost:9999                                 │
├─────────────────────────────────────────────────┤
│                  数据库层 (MySQL 8.0)              │
│                                                   │
│   30张表 + 43个外键 + 4个视图 + 8个存储过程 + 5个触发器  │
│   localhost:3306/hospital                         │
└─────────────────────────────────────────────────┘
```

| 层次 | 技术栈 | 规模 |
|------|--------|------|
| 前端 | Vue 2.6 + Element UI 2.15 + Axios + ECharts 4.8 + Vue Router 3.2 | 56 个页面 |
| 后端 | Spring Boot 2.2.4 + MyBatis-Plus 3.1.1 + JWT + Redis(Jedis) | 144 个 Java 文件 |
| 数据库 | MySQL 8.0 + InnoDB | 30 张表，43 个外键 |
| 构建 | Maven 3.9.9 (后端) + Vue CLI 3.x (前端) | Java 21 |

---

## 三、数据库总体设计

### 3.1 表结构总览（30张表，按业务域分7组）

| 域 | 表名 | 行数 | 外键 | 说明 |
|----|------|------|------|------|
| **用户与权限** | admini | 7 | 0 | 系统管理员 |
| | doctor | 16 | 1 | 医生信息 |
| | patient | 18 | 0 | 患者信息 |
| | department | 3 | 0 | 科室字典 |
| **挂号与就诊** | orders | 22 | 2 | 挂号订单（核心枢纽表） |
| | triage_record | 8 | 3 | 分诊记录（纯路由，无诊断） |
| | queue_number | 9 | 3 | 排队号码 |
| | outpatient_emr | 12 | 3 | 门诊病历 |
| | arrange | 3 | 2 | 医生排班 |
| | registrate | 3 | 2 | 挂号预约（旧表） |
| | arrangement | 2 | 0 | 排班时段（旧表） |
| **处方与药品** | prescription_master | 7 | 2 | 处方主表 |
| | prescription_detail | 11 | 3 | 处方明细(关联pm_id+dr_id) |
| | pharmacy_dispensing | 15 | 4 | 发药记录(关联presc_detail_id) |
| | drug | 14 | 0 | 药品目录 |
| | drug_batch | 8 | 1 | 药品批次（FEFO管理） |
| | inventory_transaction | 11 | 2 | 库存流水 |
| | dispensing_batch_detail | 7 | 2 | 发药批次分摊 |
| | prescribe | 3 | 0 | 旧处方（已弃用） |
| | drug_category | — | — | 药品分类 |
| | drug_price_log | — | — | 药品调价日志 |
| | prescription_review | 9 | 1 | 处方审核 |
| | prescription_template | — | — | 处方模板 |
| **检查检验** | checks | 3 | 0 | 检查项目目录 |
| | order_check | 12 | 2 | 检查开单(与处方平行) |
| | issue_check | 2 | 0 | 旧检查关联（已弃用） |
| **收费与发票** | billing_record | 8 | 1 | 收费记录 |
| | invoice_record | 11 | 2 | 发票记录(关联br_id) |
| | refund_request | 12 | 3 | 退费申请(支持oc_id逐项退) |
| **床位管理** | bed | 11 | 2 | 床位管理 |
| **辅助表** | notification | 8 | 2 | 消息通知 |
| | audit_log | 8 | 0 | 审计日志 |
| | emr_template | 10 | 0 | 病历模板 |

### 3.2 核心关系链

```
patient ──1:N──> orders ──1:1──> triage_record  (分诊路由)
                    │
                    ├──1:1──> queue_number       (排队号)
                    ├──1:1──> outpatient_emr     (病历)
                    ├──1:N──> prescription_master (处方主表)
                    │              └──1:N──> prescription_detail (处方明细)
                    │                            └──1:1──> pharmacy_dispensing (发药)
                    ├──1:N──> order_check        (检查单，与处方平行)
                    ├──1:N──> billing_record     (收费)
                    │              └──1:1──> invoice_record      (发票)
                    └──────────> refund_request  (退费，关联br_id和oc_id)
```

### 3.3 订单状态机

```
0 已挂号 → 1 已分诊 → 2 就诊中 → 3 已开单 → 4 待缴费 → 5 已缴费 → 6 已发药 → 7 完成
                                                                       ↓
                                                                     -1 已取消
```

---

## 四、视图（4个）

| 视图 | 用途 | 数据来源 |
|------|------|----------|
| `v_patient_visit_detail` | 患者就诊完整记录（挂号→医生→诊断→缴费状态） | orders + patient + doctor + outpatient_emr |
| `v_drug_stock_alert` | 药品库存预警（库存≤最小库存的药品） | drug + drug_batch(子查询) |
| `v_doctor_today_queue` | 医生今日接诊队列（实时排队情况） | doctor + queue_number + patient |
| `v_patient_billing_detail` | 患者费用明细（挂号费+药费+检查费） | patient + orders + 子查询汇总 |

---

## 五、存储过程（8个）

| 存储过程 | 参数 | 功能 |
|----------|------|------|
| `sp_daily_clinic_report` | report_date | 门诊日报：挂号总数、收入、完成/取消/爽约数 |
| `sp_doctor_workload` | start_date, end_date, doctor_id | 医生工作量：接诊人次、病历数、处方数、总收入 |
| `sp_department_income` | start_date, end_date | 科室收入：各科室挂号人次、费用合计、人均费用 |
| `sp_patient_visit_history` | patient_id | 患者就诊历史：全部就诊记录摘要 |
| `sp_drug_consumption_ranking` | start_date, end_date, top_n | 药品消耗排行：消耗量、消耗金额、处方张数 TOP N |
| `insert_orders` | — | 旧：插入订单 |
| `insert_drug` | — | 旧：插入药品 |
| `insert_check` | — | 旧：插入检查 |

---

## 六、触发器（5个）

| 触发器 | 触发时机 | 功能 |
|--------|----------|------|
| `trg_order_cancel_release_queue` | orders UPDATE (o_state=-1) | 订单取消→自动释放排队号 |
| `trg_dispensing_update_stock` | pharmacy_dispensing UPDATE (pd_status=2) | 发药后→自动扣减drug库存并写inventory_transaction流水 |
| `trg_order_auto_queue` | orders INSERT | 挂号成功→自动生成排队号（按科室前缀） |
| `trg_refund_void_invoice` | refund_request UPDATE (rf_status=1) | 退费审批通过→自动作废关联发票 |
| `trg_missed_release_queue` | orders UPDATE (o_missed=1) | 标记爽约→释放排队号+推送爽约通知 |

---

## 七、前后端接口架构

### 7.1 前端路由（4个顶级布局）

| 路由前缀 | 角色 | 子页面数 |
|----------|------|----------|
| `/login` | 公众 | 登录页 |
| `/admin` | 管理员 | 菜单组：基础数据、门诊业务、药房管理、检查检验、收费管理、床位管理、数据统计、审计日志 |
| `/doctor` | 医生 | 接诊队列、订单处理、处方开方、检查开单 |
| `/patient` | 患者 | 我的挂号、排队状态、我的病历、我的处方、我的费用 |

### 7.2 后端API（24个Controller）

| Controller | 路径 | 核心端点 |
|------------|------|----------|
| AdminController | `/admin` | login, findAllDoctors, findAllPatients, findAllOrders |
| PatientController | `/patient` | login, addOrder(挂号), findDoctorBySection |
| DoctorController | `/doctor` | login, findOrderByNull, findPatientById |
| OrderController | `/order` | processPayment, updateOrderState, finalizeConsultation, cancelOrder |
| PrescriptionController | `/prescription` | save(含处方主表+明细+发药记录自动生成) |
| PharmacyDispensingController | `/pharmacy` | findAll, dispense, review, returnDrug |
| CheckController | `/check` | createOrderCheck, batchCreateOrderChecks, updateCheckResult |
| BillingController | `/billing` | addRecord, findByOrder, dailySummary |
| InvoiceRecordController | `/invoiceRecord` | findByOrder, findByBilling, findByDate |
| RefundRequestController | `/refundRequest` | findAll, create, approve(含校验), reject |
| TriageRecordController | `/triage` | findAll, create(纯路由,不诊断) |
| QueueController | `/queue` | takeNumber, callNext, skipNumber, listByDoctor |
| DrugController | `/drug` | findAllDrugs, addDrug, modifyDrug, reduceDrugNumber |
| BedController | `/bed` | findAllBeds, addBed, emptyBed |
| EmrController | `/emr` | save, findByOrder, pdf |
| ArrangeController | `/arrange` | findByTime, addArrange, deleteArrange |
| AuditLogController | `/auditLog` | findAll |
| NotificationController | `/notification` | findByPatient, markRead, unreadCount |
| DrugBatchController | `/drugBatch` | findAll |
| DrugCategoryController | `/drugCategory` | findAll, add/modify/delete |
| InventoryController | `/inventory` | dashboard, transactions, receive, writeOff |
| DeliveryRequestController | `/delivery` | create, findByPatient, pickup |
| DiagnosisDictController | `/diagnosisDict` | findAll |
| EmrTemplateController | `/emrTemplate` | findAll, add/modify/delete |

---

## 八、关键业务流程实现

### 8.1 挂号流程
```
患者选择科室→选择医生→选择时段→系统生成订单(o_state=0)
                              ↓
          Redis Lua脚本原子扣减时段名额（失败降级跳过）
                              ↓
          触发器 trg_order_auto_queue 自动生成排队号
```

### 8.2 接诊开方流程
```
医生叫号→患者进入诊室(o_state=2)
              ↓
医生书写病历(EMR) + 开处方 + 开检查（三个并行操作）
              ↓
   ┌─────────┼─────────┐
   ↓         ↓         ↓
 EMR保存  处方保存   检查保存
          (主表+明细) (order_check)
              ↓
      自动生成发药记录
              ↓
  finalizeConsultation → o_state=3 (已开单)
```

### 8.3 缴费→发药流程
```
患者缴费→billing_record写入→invoice_record写入(关联br_id)
        ↓
    o_price_state=1 (已缴费)
        ↓
药师核对处方→dispense()校验已缴费+FEFO选批次→pd_status=1(待复核)
        ↓
另一药师review()→pd_status=2(已发药)
        ↓
trg_dispensing_update_stock→自动扣drug库存+写inventory_transaction流水
```

### 8.4 退费流程
```
创建退费申请(rf_status=0)
        ↓
审批前校验: ①订单已缴费? ②检查项未执行/未完成? ③药品未发药?
        ↓  (任一不满足→拒绝)
审批通过(rf_status=1)
        ↓
trg_refund_void_invoice→自动作废发票
```

---

## 九、安全与校验机制

| 机制 | 实现方式 |
|------|----------|
| 身份认证 | JWT Token，登录后签发，请求头携带 |
| 请求拦截 | JwtInterceptor 校验 Token 有效性 |
| 密码加密 | MD5 加盐哈希存储 |
| 跨域处理 | CorsConfig 全局 CORS 配置 |
| 状态机校验 | updateOrderState() 禁止状态回退，前5步最多跳2步 |
| 退费校验 | validateBeforeApprove() 三项前置验证 |
| 发药校验 | 必须已缴费(o_price_state=1)才能发药 |
| FEFO库存 | dispenseFefo() 按效期从早到晚依次出库 |
| 乐观锁 | bed表version字段防止并发冲突 |
| 审计日志 | 订单状态变更、病历修改自动记录audit_log |

---

## 十、修复前后对比

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| 发药关联 | pharmacy_dispensing → orders | pharmacy_dispensing → **prescription_detail** (presc_detail_id) |
| 发票关联 | invoice_record → orders | invoice_record → **billing_record** (br_id) |
| EMR与检查关系 | 交叉耦合，创建检查即推状态 | **平行分支**，finalizeConsultation统一推进 |
| 处方结构 | 无主表，明细直挂orders | **主从结构**：prescription_master → prescription_detail |
| 退费校验 | 零验证直接批准 | **三项校验**：缴费状态+检查状态+发药状态 |
| 退费粒度 | 仅整单退 | **逐项退**：br_id退收费项 + oc_id退检查项 |
| 分诊职责 | 含主诉字段（诊断功能） | **纯路由**：严重等级+生命体征+目标医生 |
| oTriage语义 | 存储挂号类型但命名为"分诊" | **oRegType**，挂号为挂号，分诊为分诊 |
| Redis队列 | 不可用时直接报错 | **try-catch降级**，失败记录警告 |
| 外键约束 | 仅4个 | **43个**（补充39个） |
| 视图 | 0个 | **4个** |
| 存储过程 | 3个（仅基础插入） | **8个**（含日报、工作量、科室收入、药品排行） |
| 触发器 | 0个 | **5个** |

---

## 十一、文件清单

### SQL 脚本
| 文件 | 说明 |
|------|------|
| `hospital_complete.sql` | 完整建库脚本（23张原表） |
| `db_migration.sql` | 原版迁移脚本 |
| `hospital_advanced_migration.sql` | 高级扩展迁移脚本 |
| `fix_migration_phase0.sql` | 阶段0：创建4张缺失表+2个ALTER |
| `course_requirements_final.sql` | 课程要求：43个FK+2视图+2存储过程+2触发器+实验数据 |
| `procedures_and_triggers.sql` | 存储过程与触发器独立脚本 |
| `additional_objects.sql` | 新增：2视图+3存储过程+3触发器 |

### 新建 Java 文件
| 文件 | 说明 |
|------|------|
| `pojo/PrescriptionMaster.java` | 处方主表实体 |
| `mapper/PrescriptionMasterMapper.java` | 处方主表Mapper |

### 修改 Java 文件（16个）
`Orders.java`, `TriageRecord.java`, `PharmacyDispensing.java`, `InvoiceRecord.java`, `RefundRequest.java`, `PrescriptionDetail.java`, `OrderService.java`, `InvoiceRecordService.java`, `RefundRequestService.java`, `OrderServiceImpl.java`, `CheckServiceImpl.java`, `PrescriptionServiceImpl.java`, `PharmacyDispensingServiceImpl.java`, `RefundRequestServiceImpl.java`, `InvoiceRecordServiceImpl.java`, `TriageRecordServiceImpl.java`, `OrderController.java`, `RefundRequestController.java`, `PrescriptionController.java`, `InvoiceRecordController.java`

### 配置文件
| 文件 | 说明 |
|------|------|
| `application.yml` | 数据库密码改为206528 |
| `.claude/launch.json` | 前后端启动配置 |
| `~/.bashrc` | Maven环境变量+UTF-8编码 |

---

## 十二、运行方式

```bash
# 1. 导入数据库
mysql -u root -p"206528" hospital < hospital_complete.sql
mysql -u root -p"206528" hospital < course_requirements_final.sql

# 2. 启动后端
java -jar Hospital-idea/Hospital/target/Hospital-1.0-SNAPSHOT.jar

# 3. 启动前端
cd HospitalVue\ 2/HospitalVue && npm run dev

# 4. 访问
# 前端: http://localhost:8089
# 管理员: 202601 / 123456
# 医生: 202401 / 123456
# 患者: 10001 / 123456
```
